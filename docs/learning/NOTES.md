# Notes

- Workspace created 2026-08-28, on branch `feature/dashboard`. Placed under `docs/learning/` inside the repo (per this project's CLAUDE.md rule: don't touch files outside `D:\dev\task-manager`). Not auto-committed — these are personal learning artifacts, not part of the app; ask the user before committing them.
- No teaching-pace or format preferences stated yet.
